name: 'Publish Blog Post'
description: 'Publishes blog post to Hashnode'
author: 'Johnkayode'


inputs:
  title:
    required: true
    description: 'Title of the post'
  subtitle:
    required: false
    description: 'Description of the post'
  cover_image:
    required: false
    description: 'Cover image url for the post'
  publication_id:
    required: true
    description: 'The ID of publication the post belongs to.'
  markdown:  
    description: 'Filepath to the markdown file to publish'
    required: true
  access_token:
    description: "Hashnode developer access token"
    required: true

outputs:
  id:
    description: The ID of the new blog post.
  url:
    description: The URL of the new blog post.

runs:
  using: 'node20'
  main: 'dist/index.js'